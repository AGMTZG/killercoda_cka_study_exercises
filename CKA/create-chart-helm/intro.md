### Creating a Helm Chart for a Database Application

Your manager has asked you to create a custom Helm chart for your company’s database application.
The goal is to package the MongoDB database into a deployable unit that can be easily managed and versioned within Kubernetes.

In this task, you will:

- Generate a new Helm chart structure for the database.

- Modify and organize the templates to include MongoDB configurations and its Service.

- Customize the values.yaml file to define parameters for the database, such as credentials and image version.

- Deploy the database into the cluster and confirm that it runs correctly.

Press Next to start.
